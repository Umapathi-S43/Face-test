# PlasticVision Pro v2 — Dependencies
# Backend (GPU server)
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# AI / Computer Vision
opencv-python>=4.8.0
numpy>=1.24.0
insightface>=0.7.3
onnxruntime-gpu>=1.16.0  # Use onnxruntime (no -gpu) for CPU-only

# Face Enhancement (optional but recommended)
gfpgan>=1.3.8
torch>=2.0.0
torchvision>=0.15.0

# Utilities
Pillow>=10.0.0
